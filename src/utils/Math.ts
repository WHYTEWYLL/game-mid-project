export const randomeNum = (max:number):number => { return Math.floor(Math.random() * max) }

export const abs = (num: number):number => { return Math.abs(num) }

export const hypot = (x:number,y:number):number => { return Math.hypot(x, y) }