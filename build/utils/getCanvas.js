// Obtiene el canvas
export const canvas = document.getElementById('canvas');
// Contexto del canvas 2D
export const ctx = canvas.getContext('2d');
// Cálculo de la mitad del canvas
export const canvasMid = { x: canvas.width / 2, y: canvas.height / 2 };
